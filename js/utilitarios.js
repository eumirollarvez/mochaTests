funcionesCalculos = {
    sumarNumeros: (a, b) => a + b,
    restarNumeros: (a, b) => a - b,
    multiplicarNumeros: (a, b) => a * b,
    dividirNumeros: (a, b) => a / b
}

module.exports = funcionesCalculos;